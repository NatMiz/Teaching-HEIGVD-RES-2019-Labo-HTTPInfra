{
  "name": "student-express",
  "version": "0.1.0",
  "description": "Generates dynamic, random content and returns a JSON payload.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "NatMiz",
  "license": "ISC",
  "dependencies": {
    "chance": "^1.0.18",
    "express": "^4.17.1"
  }
}
